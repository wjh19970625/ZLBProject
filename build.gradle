// Top-level build file where you can add configuration options com.wjh.utillibrary.common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.2'
        

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
        maven { url "https://maven.google.com" }
    }
}

ext {
    android = [
            compileSdkVersion : 27,        //根据自己项目的来配置
            buildToolsVersion : '27.1.1',  //根据自己项目的来配置
            targetSdkVersion  : 27,        //根据自己项目的来配置
            supportVersion    : '27.1.1',  //android support version
            frescoVersion     : '0.14.1'   //fresco的版本，可自行修改
    ]
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
